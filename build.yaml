# ZMK's CI workflow automatically passes the repo root as ZMK_EXTRA_MODULES,
# which picks up zephyr/module.yml at the repo root — no cmake-args needed for CI.
# For local west builds, pass: -DZMK_EXTRA_MODULES=/home/vince/Documents/zmk-config
---
include:
  - board: nice_nano_v2
    shield: kyria_rev3_left
  - board: nice_nano_v2
    shield: kyria_rev3_right
