#pragma once
#include <lvgl.h>

/*
 * virtual_pet — stub. Pet engine not yet implemented.
 * See feature/virtual-pet branch for active development.
 * Char count and WPM are in typing_stats.h.
 */

void virtual_pet_init(lv_obj_t *pet_container, lv_obj_t *status_icon, lv_obj_t *status_label);
